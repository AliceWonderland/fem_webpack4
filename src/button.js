// take string, as label
// return element
// jsdocs full typescript
/**
 * @param {string} buttonName
 * @returns {Element}
 */

// module.exports with an s
module.exports = (buttonName) => {
    return `Button: ${buttonName}`;
};